[package]
name = "@crypto/rand"
version = "0.10.1"
description = "Random number generators for Zeta — Rng trait, distributions, seq sampling"
license = "MIT"
repository = "https://github.com/murphsicles/rand"
readme = "README.md"
